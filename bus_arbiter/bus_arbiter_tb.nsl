#include "bus_arbiter.nh"

declare bus_arbiter_tb simulation {}

module bus_arbiter_tb
{
    bus_arbiter arbiter;

    reg counter[10] = 10'h0;
    counter++;

    if(counter > 10'h20)
    {
        _finish();
    }
}
