#include "csr.nh"

module control_state_registers
{
    //func_in read_write(address, write_value): read_result;
    func read_write
    {
        any
        {
            // mhartid (MRO)
            address == 0xf14:
            {
                return 32'h0;
            }
            else:
            {
                error();
            }
        }
    }

    //func_in write(address, write_value);
}
